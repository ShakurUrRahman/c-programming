#include <stdio.h>
int main()
{
    int tk;
    scanf("%d", &tk);
    if (tk >= 100)
    {
        printf("Eat burger");
    }
    else
    {
        printf("Don't eat anything");
    }
    return 0;
}